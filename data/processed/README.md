# Processed Data

This directory only includes non-sensitive processing reports.

Row-level processed tables that contain Bilibili identifiers, titles, thumbnail URLs, uploader fields, or local thumbnail paths are intentionally excluded from the public repository.
